main :: [Any] -> int
main (prog) {
    count (prog)
}

count :: [Any] -> int
count (prog) {
    if (prog[0] == "SEQ") 
        then count(prog[1]) + count(prog[2])
        else if (prog[0] == "ASGN")
            then 1
            else 0
}