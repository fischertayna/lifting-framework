main :: Any -> int
main (value) {
    chaoticIteration (value)
}

reflexive :: Any -> Any
reflexive (value) {
    value
}

chaoticIteration :: Any -> Any
chaoticIteration (x) {
    if (isEqual ((reflexive (x)), x))
        then x
        else chaoticIteration (reflexive (x))
}